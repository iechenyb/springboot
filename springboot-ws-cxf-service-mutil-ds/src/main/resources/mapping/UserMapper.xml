<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.UserDao" >
    <resultMap id="BaseResultMap" type="com.example.model.MyUser" >
        <result column="account" property="account"  jdbcType="VARCHAR" />
      <!--   <result column="password" property="password"  jdbcType="VARCHAR" /> 
      字段名相同时，不用进行字段对应
      -->
    </resultMap>
    <sql id="Base_Column_List" >
        account, password
    </sql>
    <select id="getUserByAccount" resultMap="BaseResultMap" parameterType="String" >
        select
        <include refid="Base_Column_List" />
        from t_user
        where account = #{id,jdbcType=VARCHAR}
    </select>
     <delete id="deleteByPrimaryKey" parameterType="Integer" >
        delete from t_user
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <!-- id 自增 -->
    <insert id="insertMyUser" parameterType="com.example.model.MyUser" >
        insert into t_user (account, password)
        values (#{account,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.example.model.MyUser">
        update t_user
        set password = #{password,jdbcType=VARCHAR}
        where id = #{id,jdbcType=INTEGER}
    </update>
   <!--
    <insert id="insertSelective" parameterType="com.xdd.entity.User" >
        insert into user_t
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                id,
            </if>
            <if test="userName != null" >
                user_name,
            </if>
            <if test="password != null" >
                password,
            </if>
            <if test="age != null" >
                age,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                #{id,jdbcType=INTEGER},
            </if>
            <if test="userName != null" >
                #{userName,jdbcType=VARCHAR},
            </if>
            <if test="password != null" >
                #{password,jdbcType=VARCHAR},
            </if>
            <if test="age != null" >
                #{age,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.xdd.entity.User" >
        update user_t
        <set >
            <if test="userName != null" >
                user_name = #{userName,jdbcType=VARCHAR},
            </if>
            <if test="password != null" >
                password = #{password,jdbcType=VARCHAR},
            </if>
            <if test="age != null" >
                age = #{age,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
     -->
</mapper>